# Society0

## 项目定位

Society0 是通用的多主体仿真引擎。它负责运行步骤、主体身份与状态、环境接入、FoV、Action、模型调用、Agent Thread、记忆、并发激活、结果发布和完整检查点。产业、税务、金融或其他领域规则应由外部 Environment 提供。

## 核心边界

- `World` 保存可恢复的主体与环境状态；`StepRuntimeScope` 保存只在当前步骤有效的索引、游标和去重集合，不进入检查点。
- 一个步骤只有完整成功后才发布结果与 complete marker。未处理异常留下诊断信息，恢复必须从上一完整检查点创建新的引擎实例。
- Agent Thread 保存模型请求、响应、工具调用和结果。记忆写入在提交后的 Thread 上提取；自动召回、自动写入和显式记忆工具是相互独立的能力。
- Action 的可用范围由环境注册和主体类型决定。Society0 负责参数校验、预算与调用边界，不替领域环境判断业务是否合法。
- Provider 并发、超时、输出长度和请求参数是运行合同的一部分。工具调用是否成功要以结构化结果为准，不能从模型正文猜测。

## 状态访问方式

Society0 提供两种独立的环境状态访问方式：

- `transparent_proxy`：默认方式。环境按普通映射接口读写，由状态代理记录增量变化。
- `explicit_transactions`：可选方式。普通读取返回只读视图，写入通过显式事务集中提交和校验。

两种方式共享同一业务语义与持久化结果。显式事务不会替代透明代理，外部环境只有在明确选择并通过相同端到端测试后才能使用。

## 持久化与恢复

当前主线使用声明式增量检查点：可替换状态保存最新值，追加事实保存新段，Agent Thread、记忆和 World 在同一步边界配对。长期运行不得在每次小修改后序列化整个 World，也不得依靠失败进程的内存继续推进。

完整检查点、运行配置和应用合同共同决定能否恢复。恢复时可以复用已经验证的状态，不能为了防御性检查反复深拷贝或重新序列化同一份大型数据。

## 发布标准

发布前至少满足：确定性全量测试通过；两种状态模式的共享语义测试通过；完整检查点、恢复、分叉、Thread 和记忆配对测试通过；发行版本、标签、README 和锁文件一致。真实模型测试另行验证具体 Provider，不纳入默认确定性测试。

项目使用与 API 入口见 `README.md`，版本变更见 `docs/release-*.md`。
