# Society0 4.1.2

4.1.2 细化 OpenAI 兼容端点的输出截断语义。

- `finish_reason=length` 且没有完整工具调用时，Agent 回合仍然失败，
  Inbox 不会被确认，截断内容不会被当成有意的不行动。
- 端点已返回完整、可解析的结构化工具调用时，Society0 会丢弃被截断的
  正文与推理内容，继续按工具 schema、行动权限与调用预算验证并执行
  该工具。半截文字不会进入对话历史或长期记忆。

这个调整不把截断等同于回合完成，只保留端点已经完整交付的结构化
行动。
