# Society0 4.1.5

## 修复

- 恢复到新运行目录时，可以直接使用来源检查点已经冻结的应用合同摘要。
  运行时仍会重新核对 LLM、Embedding、并发设置和能力 schema；应用合同
  摘要只用于延续同一条已验证的运行谱系，避免根据恢复时的可变世界状态
  重新计算摘要而错误拒绝合法恢复。

## 验证

- 新增完整检查点到新目录的摘要恢复测试。
- 保留不同应用合同必须拒绝恢复的原有测试。
