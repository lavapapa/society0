# Society0 4.1.4

4.1.4 修正真实模型在记忆提炼工具参数中陷入重复空白后，恢复回合继续复述
同一段坏参数的问题：

- 未通过校验的模型响应仍完整保留在审计历史中，但不再注入恢复请求；
- 恢复提示明确要求 `memories` 直接使用 JSON 数组，禁止把数组再次编码为字符串；
- 每次最多提炼 5 条、每条不超过 500 字，并要求概括带引号、转义符、JSON 或
  表格的原文，避免再次耗尽输出上限。

记忆仍由 Agent 根据完整 Thread 使用大模型提炼；本次调整不生成替代摘要，也不把
记忆写入简化为纯向量化。
