# Society0 4.1.3

4.1.3 缩短复杂环境使用声明式增量持久化时的状态读写开销：

- 复用浅层状态容器代理，避免反复创建相同包装对象；
- 将持久化声明编译成按路径查询的前缀树；
- 事实表直接写入不再遍历全部动态规则；
- 增量封存后直接交给发布器，移除写盘前后成对出现的递归容器转换。

检查点格式、恢复语义、Agent Thread 和 Memory 可见性不变。产业链同一检查点
七日重放的经济状态保持一致，自动经营阶段由 169.86 秒缩短至 104.14 秒。
